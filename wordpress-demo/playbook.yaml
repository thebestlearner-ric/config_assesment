---
- hosts: all
  gather_facts: false
  become: yes

  tasks:
    - name: install python 2
      raw: test -e /usr/bin/python || (apt -y update && apt install -y python-minimal)

    - hosts: wordpress
      roles:
        cod e- server
        - php
        - mysql
        - wordpress